# qodana.yaml
version: "1.0"

profile:
  name: qodana.starter

# ✅ valid python linter
linter: jetbrains/qodana-python:latest

# optional: kuch folders skip
exclude:
  - name: All
    paths:
      - .venv
      - __pycache__
      - uploads
      # - app.py   # (agar is file ko bhi skip karna ho)

